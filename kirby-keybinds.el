(defvar kirby-leader-key "SPC"
  "Leader key for all Kirbymacs custom keybinds")

(defvar kirby-localleader-key "SPC m"
  "The localleader prefix key, for major-mode specific commands.")

(defvar kirby-leader-map (make-sparse-keymap)
  "An overriding keymap for <leader> keys.")
